// export default function () { // default 통로 (기본 통로) 로 나가는 것이기 때문에 뒤에 이름이 필요 없다.
export function gotit() {
  return Math.floor(Math.random() * 10)
}

export const user = {
  name: 'heropy',
  age: 29
}

export default 123